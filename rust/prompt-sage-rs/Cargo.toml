[package]
name = "prompt-sage-rs"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust migration scaffold for prompt-sage CLI"

[dependencies]
clap = { version = "4.5.9", features = ["derive"] }
regex = "1.10.5"
